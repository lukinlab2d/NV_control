"""
This file is part of B00 codes based on b26_toolkit. Questions are addressed to Hoang Le.
"""
import pyvisa as visa
import sys
from qcodes.instrument.base import Instrument
from qcodes.instrument.parameter import Parameter
import time, dropbox

class AG33522A():
    def __init__(self, name='AG33522A', AGnum=1, settings=None, **kwargs) -> None:
        rm = visa.ResourceManager()
        self.AGnum = AGnum
        if AGnum == 1:
            self.AG = rm.open_resource('USB0::0x0957::0x2307::MY50001552::INSTR') # Agilent 33522A phase-bandwidth tunable AWG
            self.AG.query('*IDN?')

    def set_function(self, function, channel=1):
        self.AG.write('SOUR' + str(channel) + ':FUNC ' + function)

    def set_freq(self, freq, channel=1, units='Hz'):
        self.AG.write('SOUR' + str(channel) + ':FREQ:MODE CW')
        self.AG.write('SOUR' + str(channel) + ':FREQ ' + str(freq))
    
    def set_amplitude(self, amplitude, channel=1, units='mVpp'):
        self.AG.write('SOUR' + str(channel) + ':VOLT ' + str(amplitude) + ' ' + units)

    def set_DCoffset(self, DCoffset, channel=1, units='mV'):
        self.AG.write('SOUR' + str(channel) + ':VOLT:OFF ' + str(DCoffset) + ' ' + units)
    
    def enable_RFOutput(self, channel=1):
        self.AG.write('OUTP' + str(channel) + ' ON')

    def disable_RFOutput(self, channel=1):
        self.AG.write('OUTP' + str(channel) + ' OFF')
    
    def apply(self, channel=1, function='SIN', freq=2e6, amplitude=0.01, DCoffset=0):
        self.AG.write('SOUR' + str(channel) + ':APPL:' + function + ' ' + str(freq) + ',' + str(amplitude) + ',' + str(DCoffset))
    
    def turn_off(self):
        self.disable_PM(channel=1);       self.disable_PM(channel=2)
        self.disable_RFOutput(channel=1); self.disable_RFOutput(channel=2)
    
    def set_PMsource(self, channel=1, source='INT'):
        self.AG.write('SOUR' + str(channel) + ':PM:SOUR ' + source)
    def set_PMfunction(self, channel=1, function='NOIS'):
        self.AG.write('SOUR' + str(channel) + ':PM:INT:FUNC ' + function)
    def set_PMfreq(self, channel=1, freq=1e3):
        self.AG.write('SOUR' + str(channel) + ':PM:INT:FREQ ' + str(freq))
    def set_PMdeviation(self, channel=1, dev=90):
        self.AG.write('SOUR' + str(channel) + ':PM:DEV ' + str(dev))
    def enable_PM(self, channel=1):
        self.AG.write('SOUR' + str(channel) + ':PM:STAT ON')
    def disable_PM(self, channel=1):
        self.AG.write('SOUR' + str(channel) + ':PM:STAT OFF')
    def phase_sync(self, channel=1):
        self.AG.write('SOUR' + str(channel) + ':PHAS:SYNC')

    def set_noiseBandwidth(self,channel=1,bandwidth=20e3):
        self.AG.write('SOUR' + str(channel) + ':FUNC:NOIS:BAND ' + str(bandwidth))

    def doExperiment(self,ifTestSig, ifRndPhaseNoise, ifDiffFreqExp, singleChannel=1, AGBW= 25e3,
                      AGfreq=2.5e6, AGamp=0.2, AGfreq2=2.083333e6, AGamp2=0):
        if ifDiffFreqExp==0:
            if ifTestSig==1:
                self.turn_off()
                time.sleep(1)
                
                if ifRndPhaseNoise==1:
                    self.set_PMsource(channel=singleChannel)
                    self.set_PMfunction(channel=singleChannel,function='NOIS')
                    self.set_PMdeviation(channel=singleChannel)
                    self.set_noiseBandwidth(channel=singleChannel,bandwidth=AGBW)

                self.apply(channel=singleChannel,function='SIN', freq=AGfreq, amplitude=AGamp, DCoffset=0)

                if ifRndPhaseNoise==1: 
                    self.enable_PM(channel=singleChannel)
            else:
                self.turn_off()
        elif ifDiffFreqExp==1:
            if ifTestSig==1:
                self.turn_off()
                time.sleep(1)

                self.AG.write('UNIT:ANGL DEG')
                self.AG.write('SOUR2:PHAS 0')
                self.AG.write('PHAS:REF')
                
                if ifRndPhaseNoise==1:
                    self.set_PMsource(channel=1)
                    self.set_PMfunction(channel=1,function='NOIS')
                    self.set_PMdeviation(channel=1)
                    self.set_noiseBandwidth(channel=1,bandwidth=AGBW)
                else:
                    self.phase_sync()
                    print('Phase synced')

                self.apply(channel=1, function='SIN', freq=AGfreq,  amplitude=AGamp,  DCoffset=0)
                self.apply(channel=2, function='SIN', freq=AGfreq2, amplitude=AGamp2, DCoffset=0)

                if ifRndPhaseNoise==1:
                    self.enable_PM(channel=1)
            else:
                self.turn_off()
        

# if __name__ == '__main__':

#     ifTestSig=1; ifRndPhaseNoise=1; ifDiffFreqExp=0
#     AGBW = 25e3; AGfreq = 2.5e6; AGamp = 0.2; AGfreq2 = 2.5e6; AGamp2 = 0.2

#     AG = AG33522A()
#     AG.doExperiment(ifTestSig, ifRndPhaseNoise, ifDiffFreqExp, AGBW=AGBW,
#                       AGfreq=AGfreq, AGamp=AGamp, AGfreq2=AGfreq2, AGamp2=AGamp2)
    
    # if ifDiffFreqExp==0:
    #     if ifTestSig==1:
    #         AG = AG33522A()
    #         AG.disable_PM()
    #         AG.disable_RFOutput()
    #         time.sleep(1)
            
    #         if ifRndPhaseNoise==1:
    #             AG.set_PMsource()
    #             AG.set_PMfunction(function='NOIS')
    #             AG.set_PMdeviation()
    #             AG.set_noiseBandwidth(bandwidth=AGBW)

    #         AG.apply(function='SIN', freq=AGfreq, amplitude=AGamp, DCoffset=0)

    #         if ifRndPhaseNoise==1: 
    #             AG.enable_PM()
    #     else:
    #         AG = AG33522A()
    #         AG.disable_PM()
    #         AG.disable_RFOutput()
    # else:
    #     if ifTestSig==1:
    #         AG = AG33522A()
    #         AG.disable_PM(channel=1);       AG.disable_PM(channel=2)
    #         AG.disable_RFOutput(channel=1); AG.disable_RFOutput(channel=2)
    #         time.sleep(1)
            
    #         if ifRndPhaseNoise==1:
    #             AG.set_PMsource(channel=1)
    #             AG.set_PMfunction(channel=1,function='NOIS')
    #             AG.set_PMdeviation(channel=1)
    #             AG.set_noiseBandwidth(channel=1,bandwidth=AGBW)
    #         else:
    #             AG.phase_sync()

    #         AG.apply(channel=1, function='SIN', freq=AGfreq, amplitude=AGamp, DCoffset=0)
    #         AG.apply(channel=2, function='SIN', freq=AGfreq2, amplitude=AGamp2, DCoffset=0)

    #         if ifRndPhaseNoise==1:
    #             AG.enable_PM(channel=1)
    #     else:
    #         AG = AG33522A()
    #         AG.disable_PM(channel=1);       AG.disable_PM(channel=2)
    #         AG.disable_RFOutput(channel=1); AG.disable_RFOutput(channel=2)
    